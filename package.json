{
  "name": "isms-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Early-preview CLI-first ISMS-P readiness assistant with local-first evidence handling.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "ismsp": "./dist/cli.js",
    "isms-agent": "./dist/cli.js"
  },
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.json && cp -R dist/src/. dist/ && node -e \"require('node:fs').chmodSync('dist/cli.js', 0o755)\"",
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "check": "tsc -p tsconfig.json --noEmit",
    "scan:public": "node scripts/public-safety-scan.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
